# 优化颜色图标工具
- 顶部head 放到“颜色方案”页上面
- 2个tab 页面，把“图标编辑”放到第一个位置
- 修改导出json，支持json命名，修改json格式，增加id字段，字段内容为json名字
    ```json 
         {
            "id": "1",   //新增的id参数，名字是json文件名
            "icon_pack_name": "",
            "pre_method": "",
        ...
         }    
    
    ```
